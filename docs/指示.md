## 1回目
README.md と docs 配下の指示に従って、
以下の最小構成でプロジェクトを作成してください。

- ASP.NET Core Minimal API
- Blazor WebAssembly
- MudBlazor
- 読み取り専用
- GitLab 連携はまだダミーデータでよい

今回は過剰なレイヤ分割を避けてください。
今はVisual Studioで作成したデフォルトテンプレートの状態です。
実現にあたり不要なコード、ディレクトリは編集・削除可能です。

まずは以下のみ作成してください。

- solution
- Api project
- Web project
- /api/summary
- /api/issues
- /api/milestones
- /api/gantt
- Web 側の Dashboard ページ

GitLab 実接続は次のステップで追加します。

## 2回目
docs/gitlab-integration.md を参照してください。

Phase1 の Step1〜Step4 を実装してください。

要件
- GitLabOptions を作成
- GitLabApiClient を作成
- appsettings から設定を読む
- /api/gitlab/test endpoint を作る
## 3回目
docs/gitlab-integration.md を参照してください。

次は Phase 2 を進めてください。
まずは project milestone を対象にしてください。
group milestone はまだ実装しないでください。

実装対象:
1. GET /api/gitlab/projects
   - groups/{groupId}/projects を呼び出す
2. GET /api/gitlab/milestones
   - 取得した各 projectId に対して projects/{projectId}/milestones を呼び出す
   - project name を付けて返す
3. GET /api/gitlab/issues
   - 取得した各 projectId に対して projects/{projectId}/issues を呼び出す
   - project name, milestone title, assignee name を可能な範囲で付けて返す

要件:
- まずはページネーション未対応でもよい
- まずは DTO を最小限で作る
- Web 側はまだ大きく変えず、API の JSON 確認を優先する
- Aspire AppHost と ServiceDefaults は維持する

## 4回目
docs/gitlab-integration.md を参照してください。

Phase 3 を実装してください。

対象:
- Step 9: DashboardIssue DTO
- Step 10: DashboardMilestone DTO
- Step 11: issue 変換
- Step 12: milestone 集計
- Step 13: /api/issues 差し替え
- Step 14: /api/milestones 差し替え
- Step 15: /api/summary 差し替え

要件:
- project milestone のみ対象
- group milestone はまだ対象外
- pagination はまだ不要
- Web 側の大きな変更は行わない

## 5回目
docs/gitlab-integration.md を参照してください。
phase 3 のstep16を実行してください